type Weather {
  city: String!
  weather: String!
  temperature: Float!
}

type Repository {
  name: String!
  description: String
  url: String!
}

type Query {
  getWeather(city: String!): Weather
  getRepositories(userName: String!): [Repository]
}
